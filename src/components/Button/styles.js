import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '70%',
        height: 48,
        flexDirection: 'row',
        alignItems: 'center',
        marginVertical: 5,
        borderRadius: 8
    },
    txtButton: {
        flex: 1,
        color: 'white',
        fontSize: 20,
        textAlign: 'center',
        fontWeight: 'bold',
    },
});